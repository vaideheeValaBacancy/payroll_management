# Dual-model AI anomaly detection engine
# Note: the Reconstruction Autoencoder is implemented in pure NumPy
# (autoencoder.py) so PyTorch is NOT required — keeps Python 3.13 support.
fastapi
uvicorn[standard]
scikit-learn
shap
xgboost
numpy
pandas
